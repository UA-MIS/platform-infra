# ⚠ SECURITY — PLACEHOLDER ciphertext. Reseal with a REAL Vault token scoped to the
# `tenant-provisioner` policy (write policy + auth/kubernetes/role under tenants/*
# ONLY) before go-live (see creds/README.md). This stub will NOT decrypt;
# provider-vault sits unauthenticated (safe failure mode) until the operator reseals.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: vault-provider-creds
  namespace: crossplane-system
  labels:
    platform.capstone/component: platform
spec:
  encryptedData:
    credentials: PLACEHOLDER_RESEAL_AT_GO_LIVE_vault_provisioner_json_DO_NOT_USE
  template:
    metadata:
      name: vault-provider-creds
      namespace: crossplane-system
    type: Opaque
