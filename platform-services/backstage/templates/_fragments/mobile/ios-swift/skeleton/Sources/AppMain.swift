import SwiftUI

// SwiftUI app entry point. The single screen lives in ContentView.swift.
@main
struct AppMain: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
