# Test/lint-only dependencies (NOT in the runtime image). The platform CI's Python checks
# step installs pytest+ruff itself; conftest.py installs requirements.txt + this file so
# tests can import the app + use the Flask test client. Keep test deps OUT of
# requirements.txt so the runtime image stays lean.
-r requirements.txt
pytest==9.0.3
ruff==0.8.4
