# Runtime dependencies (baked into the image — see Dockerfile).
# Pinned for reproducible builds; bump deliberately.
Flask==3.1.3
gunicorn==23.0.0
SQLAlchemy==2.0.36
# Pure-Python MySQL driver — no system libs / compiler, so the image stays slim and
# apt-free. The MySQL DSN is mysql+pymysql://user:pass@host:3306/db (see app/db.py).
PyMySQL==1.1.1
