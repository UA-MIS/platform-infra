# ⚠ SUPERSEDED — Phase-4 imaging is TALOS, not k3s (D-024 / ADR-017: the real
# cluster runs Talos + upstream k8s, deviating from the old D-002 "real cluster is
# k3s"). This stub is kept only to redirect; the real Phase-4 target lives in:
#
#     clusters/real-talos/        <-- talconfig.yaml (talhelper) + values.env + patches/
#     docs/phase-4-runbook.md     <-- burn / probe / 3-node install runbook
#
# Fill clusters/real-talos/values.env (PLATFORM_DOMAIN / REGISTRY / GITHUB_ORG +
# the Talos SCHEMATIC_ID / OVERLAY) and follow the runbook. Do NOT add values here.
