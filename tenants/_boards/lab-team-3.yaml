# Sprint board for team `lab-team-3` — MIS 521 lab team.
#
# ⚠ PLACEHOLDER NAME. The team is expected to be renamed. Renaming means
# renaming BOTH this file and the `team:` key below in one commit — the
# generator refuses a file whose stem and `team:` disagree, which is what stops
# a team quietly acquiring two boards. The host, the Dex redirect URI and
# APP_URL all derive from the slug, so they move together; the old board's data
# does not, so treat a rename as provisioning a new board and retiring this one.
#
# ⚠ The GitHub team `UA-MIS/lab-team-3` DOES NOT EXIST YET (checked against the
# API when this was written, 404). The board deploys and passes readiness
# regardless — the team is only consulted at sign-in — so until it exists, every
# member sees a 403 rather than the board.
#
# The repo below does not exist yet either. That one is harmless: it only scopes
# branch creation and PR linking, both of which fail soft.
#
# Rendered by applicationsets/agile-boards-appset.yaml into the shared
# `agile-boards` namespace, from the ONE shared image. Host is derived:
# lab-team-3-agile.capstone.uamishub.com
#
# De-provision = `git rm` this file.
team: lab-team-3
repos: UA-MIS/lab-team-3
title: Lab Team 3 Board
accent: "#9e1b32"
