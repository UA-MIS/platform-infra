# Sprint board for team `ss-evan-and-emily` — MIS 521 lab team.
#
# ⚠ The GitHub team `UA-MIS/ss-evan-and-emily` DOES NOT EXIST YET (checked
# against the API when this was written, 404). The board will deploy and pass
# readiness regardless — the team is only consulted at sign-in — so until the
# team exists, every member sees a 403 rather than the board. Create the team,
# then sign in; nothing here needs to change when it appears.
#
# The repo below does not exist yet either. That one is harmless: it only scopes
# branch creation and PR linking, both of which fail soft.
#
# Rendered by applicationsets/agile-boards-appset.yaml into the shared
# `agile-boards` namespace, from the ONE shared image. Host is derived:
# ss-evan-and-emily-agile.capstone.uamishub.com
#
# De-provision = `git rm` this file.
team: ss-evan-and-emily
repos: UA-MIS/ss-evan-and-emily
title: SS Evan and Emily Board
accent: "#9e1b32"

# Seeds this board's homepage with the shipped VM handover reference sheet
# (chart/files/homepage/mis521-vm-handover.md), rendered with THIS team's slug —
# the document names <team>-console.uamishub.com, so a shared copy would send a
# student to another team's VM. Read only when the homepage has never been
# edited; the team's own edits always win. Remove this line and the board falls
# back to the app's built-in starter text.
homepage: mis521-vm-handover
